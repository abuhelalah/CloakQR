# Plugin Stubs

This directory holds compile-time stub implementations for paid plugins.
The public repository keeps these as non-functional placeholders so the build
layout matches the advanced plan without shipping proprietary code.
